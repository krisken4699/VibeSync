# 🎵 VibeSync — คู่มือติดตั้งฉบับคนไม่สายเทค

> คู่มือนี้จะพาคุณตั้งแต่เริ่มต้นจนถึงเว็บไซต์ที่ใช้งานได้จริง  
> ไม่ต้องมีประสบการณ์เขียนโค้ดมาก่อน แค่ทำตามทีละขั้นได้เลย  
> ใช้เวลาประมาณ **30–45 นาที**

---

## 📋 สิ่งที่ต้องมีก่อนเริ่ม

- คอมพิวเตอร์ (Windows หรือ Mac ก็ได้)
- บัญชี GitHub (ถ้ายังไม่มีจะสอนสร้างด้านล่าง)
- บัญชี Spotify (ต้องมีอยู่แล้วแหละ 😄)
- อีเมลสำหรับสมัครบริการต่างๆ

---

## ขั้นตอนที่ 1 — สร้างบัญชี GitHub

GitHub คือที่เก็บโค้ด เหมือน Google Drive แต่สำหรับโปรเจกต์

1. ไปที่ **[github.com](https://github.com)**
2. กด **Sign up** มุมขวาบน
3. กรอกอีเมล รหัสผ่าน และชื่อผู้ใช้
4. ยืนยันอีเมล แล้วล็อกอินเข้ามา

---

## ขั้นตอนที่ 2 — Fork โปรเจกต์ (คัดลอกโค้ดมาเป็นของตัวเอง)

1. ไปที่ลิงก์ GitHub ของ VibeSync ในคำอธิบายวิดีโอ
2. กดปุ่ม **Fork** มุมขวาบนของหน้า

   ![fork button บริเวณมุมขวาบน]

3. กด **Create fork** ทิ้งไว้ default ได้เลย
4. ตอนนี้คุณมีโค้ดเป็นของตัวเองแล้ว ที่อยู่จะเป็น `github.com/ชื่อคุณ/vibesync`

---

## ขั้นตอนที่ 3 — สร้าง Spotify Developer App

นี่คือการขอ "กุญแจ" เพื่อให้เว็บเราเชื่อมต่อ Spotify ได้

1. ไปที่ **[developer.spotify.com](https://developer.spotify.com)**
2. ล็อกอินด้วยบัญชี Spotify ปกติ
3. กดชื่อตัวเองมุมขวาบน → **Dashboard**
4. กด **Create app** (ปุ่มสีเขียว)
5. กรอกข้อมูลดังนี้:
   - **App name:** `VibeSync` (หรือชื่ออะไรก็ได้)
   - **App description:** `Mood-based music discovery app`
   - **Redirect URIs:** พิมพ์ `http://localhost:5173` แล้วกด **Add**
     > ⚠️ อันนี้ต้องเพิ่มก่อน จะเพิ่มที่อยู่จริงทีหลังตอน deploy
   - ติ๊กถูก **Web API**
6. กด **Save**

### เก็บ Client ID และ Client Secret

หลังสร้าง app แล้ว:

1. กดเข้าไปใน app ที่เพิ่งสร้าง
2. กด **Settings** มุมขวาบน
3. จะเห็น **Client ID** — คัดลอกเก็บไว้ในโน้ต
4. กด **View client secret** — คัดลอกเก็บไว้ในโน้ตด้วย

> 🔐 อย่าบอก Client Secret ใคร มันเหมือนรหัสผ่านของ app คุณ

---

## ขั้นตอนที่ 4 — สร้าง Google AI (Gemini) API Key

Gemini ใช้ช่วยวิเคราะห์รสนิยมเพลงและแปล mood ที่พิมพ์เป็นภาษาคนให้เป็น genre

1. ไปที่ **[aistudio.google.com](https://aistudio.google.com)**
2. ล็อกอินด้วยบัญชี Google
3. กด **Get API key** ในเมนูซ้ายมือ
4. กด **Create API key**
5. เลือก **Create API key in new project**
6. คัดลอก key ที่ได้เก็บไว้ในโน้ตด้วย

> ✅ Gemini มี free tier ใช้ฟรีได้ ไม่ต้องใส่บัตรเครดิต

---

## ขั้นตอนที่ 5 — สมัคร Vercel และ Deploy

Vercel คือที่ที่เว็บจะอยู่ มีฟรี tier ใช้งานได้สบายๆ

### 5.1 สมัคร Vercel

1. ไปที่ **[vercel.com](https://vercel.com)**
2. กด **Sign Up** → เลือก **Continue with GitHub**
3. อนุญาตให้ Vercel เข้าถึง GitHub ได้

### 5.2 Import โปรเจกต์

1. หลังล็อกอินแล้ว กด **Add New...** → **Project**
2. จะเห็นรายการ repositories จาก GitHub ของคุณ
3. หา **vibesync** แล้วกด **Import**
4. หน้า Configure Project จะขึ้นมา — **ยังอย่ากด Deploy**

### 5.3 ตั้งค่า Environment Variables

นี่คือการใส่ "กุญแจ" ทั้งหมดที่เก็บไว้ในโน้ตเข้าไป

เลื่อนลงมาที่ส่วน **Environment Variables** แล้วเพิ่มทีละตัว:

| Name                     | Value                             |
| ------------------------ | --------------------------------- |
| `VITE_SPOTIFY_CLIENT_ID` | Client ID จาก Spotify             |
| `VITE_GEMINI_API_KEY`    | API Key จาก Google AI Studio      |
| `VITE_REDIRECT_URI_PROD` | ยังว่างไว้ก่อน จะกลับมาใส่ทีหลัง  |
| `SPOTIFY_CLIENT_ID`      | Client ID จาก Spotify (ใส่อีกรอบ) |
| `SPOTIFY_CLIENT_SECRET`  | Client Secret จาก Spotify         |

> ⚠️ `VITE_REDIRECT_URI_PROD` ต้องใส่ URL ของเว็บที่ deploy แล้ว ซึ่งเราจะได้หลัง deploy ครั้งแรก ตอนนี้ปล่อยว่างไว้ก่อนได้

### 5.4 Deploy ครั้งแรก

1. กด **Deploy**
2. รอสักครู่ Vercel จะ build โปรเจกต์
3. เมื่อเสร็จจะได้ URL เช่น `https://vibesync-abc123.vercel.app`
4. **คัดลอก URL นี้ไว้**

---

## ขั้นตอนที่ 6 — ใส่ URL กลับเข้าไป

ต้องบอกทั้ง Spotify และ Vercel ว่าเว็บอยู่ที่ไหน

### 6.1 เพิ่ม Redirect URI ใน Spotify

1. กลับไปที่ **[developer.spotify.com](https://developer.spotify.com)** → Dashboard → app ของคุณ
2. กด **Settings**
3. ที่ **Redirect URIs** กด **Add**
4. ใส่ URL ที่ได้จาก Vercel เช่น `https://vibesync-abc123.vercel.app`
5. กด **Add** แล้วกด **Save** ด้านล่าง

### 6.2 ใส่ URL ใน Vercel

1. กลับไปที่ **[vercel.com](https://vercel.com)** → โปรเจกต์ของคุณ
2. ไปที่ **Settings** → **Environment Variables**
3. หา `VITE_REDIRECT_URI_PROD` แล้วกดแก้ไข
4. ใส่ URL เช่น `https://vibesync-abc123.vercel.app`
5. กด **Save**

### 6.3 Redeploy

1. ไปที่ **Deployments** ในโปรเจกต์ Vercel
2. กดปุ่ม **...** ข้างๆ deployment ล่าสุด
3. กด **Redeploy**
4. รอสักครู่ แล้วเว็บจะพร้อมใช้งานจริงๆ

---

## ขั้นตอนที่ 7 — เพิ่มเพื่อนเข้า Spotify (สำคัญมาก!)

Spotify อนุญาตให้มีผู้ใช้ได้แค่ **5 คนรวมตัวคุณ** ถ้า app ยังไม่ได้รับการอนุมัติเป็นทางการ แต่ละคนต้อง **มี Spotify Premium** ด้วย

### วิธีเพิ่มเพื่อน

1. ไปที่ **[developer.spotify.com](https://developer.spotify.com)** → Dashboard → app ของคุณ
2. กด **Settings**
3. เลื่อนลงมาที่ **User Management**
4. กรอก **ชื่อ** และ **อีเมล Spotify** ของเพื่อน
5. กด **Add User**
6. เพื่อนจะได้รับอีเมลเชิญ ให้กด accept ในอีเมล

> 📧 อีเมลที่ใส่ต้องเป็นอีเมลที่เพื่อนใช้สมัคร Spotify นะ ไม่ใช่อีเมลอื่น

### ถ้าเพื่อนล็อกอินไม่ได้

- เช็คว่าเพื่อนกด accept ในอีเมลเชิญหรือยัง
- เช็คว่าเพื่อนมี Spotify Premium
- เช็คว่าอีเมลที่ใส่ตรงกับที่ใช้สมัคร Spotify

---

## ขั้นตอนที่ 8 — ทดสอบ

1. เปิด URL ของคุณในเบราว์เซอร์
2. กด **Connect Spotify** มุมขวาบน
3. ล็อกอิน Spotify แล้วกด Agree
4. เลือก mood แล้วดูว่าเพลงขึ้นมาไหม 🎵

---

## 🆘 ปัญหาที่เจอบ่อย

**เพลงไม่ขึ้น / "No tracks found"**
→ เช็ค `VITE_SPOTIFY_CLIENT_ID` และ `SPOTIFY_CLIENT_SECRET` ว่าใส่ถูกไหม  
→ เช็คว่า Redirect URI ใน Spotify ตรงกับ URL ของ Vercel พอดี

**ล็อกอิน Spotify แล้ว error**
→ เช็คว่า `VITE_REDIRECT_URI_PROD` ใน Vercel ตรงกับ URL ของเว็บพอดี ไม่มี `/` ต่อท้าย

**เพื่อนล็อกอินไม่ได้**
→ ต้องเพิ่มอีเมลเพื่อนใน Spotify User Management ก่อน (ขั้นตอนที่ 7)  
→ ถ้าเพื่อนไม่มี Spotify Premium ก็จะล็อกอินไม่ได้  
→ ถ้าเต็ม 5 คนแล้วก็เพิ่มไม่ได้อีก

**Build failed ใน Vercel**
→ ลองกด Redeploy อีกรอบ  
→ ถ้ายังไม่ได้ เช็คว่า environment variables ทุกตัวใส่ครบหรือยัง

---

## 📱 แชร์ให้เพื่อน

เพื่อนที่ไม่ได้รับสิทธิ์ล็อกอิน ยังใช้งานได้นะ เพียงแต่:

- ✅ เลือก mood และรับเพลงได้ปกติ
- ✅ ดูเพลงและเปิดใน Spotify ได้
- ❌ เพลงจะไม่ได้ถูก personalize ตามรสนิยม
- ❌ บันทึก playlist เข้า Spotify ไม่ได้

แค่ส่ง URL ให้เพื่อนเปิดในเบราว์เซอร์ได้เลย ไม่ต้องติดตั้งอะไรทั้งนั้น

---

\-สร้างด้วย ❤️ และ AI (2/3/2026)
